
class Model {

  constructor(vao, ebo, numIndices, modelMatrixBuffer) {
    this.vao = vao;
    this.ebo = ebo;
    this.numIndices = numIndices;
    this.modelMatrixBuffer = modelMatrixBuffer;
  }
}